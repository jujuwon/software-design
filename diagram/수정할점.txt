클래스 수정
1. DBConnect 클래스에서 DBConnect() -> DBconnect(), DBClose() -> DBclose() 함수명 변경
2. getProducts(string keyword)

시퀀스 수정
1. DBclose 비동기
2. 작년처럼 ref 밖에 excuteQuery()랑 return data 하려다 ref에 arguments, returnValue 적을 수 있는 칸이 있길래 그걸로 대체함

시퀀스 해결해야 될 사항
1 invite 유스케이스에서는 공유 대상을 선택하는 과정이 있는데 우리 과정으로 하면 그걸 나타낼수가 없음
  다시 저번처럼 연락처를 가져와서 선택하게 해야될 거 같음.(상의하고 수정)
2. deleteSharedCart() 함수에서 비번이 틀리면 재입력 받는걸 보여주기 위해서 comparePW함수를 만들지?(상의하고 수정)
3. 다른 조 처럼 ref 이름 제대로 나오게 해야됨(지금은 잘 모르겠어서 노트로 해 둠)

궁금한점
1. db커넥트에서 성공여부 반환할때 어떻게 결과를 반환하는지?
2. userID 어떻게 set하는지?